apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cycler.fullname" . }}-test-connection"
  labels:
    {{- include "cycler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cycler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
